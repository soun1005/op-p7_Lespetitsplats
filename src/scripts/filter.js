import { displayData } from './index.js';
import recipeData from '../data/recipes.js';

// filter list
ingList = document.querySelectorAll('.ing-list-element');
appList = document.querySelectorAll('.ing-list-element');
utenList = document.querySelectorAll('.ing-list-element');

function filterByTag(event){
    // 1. 필터를 누르면 -> 태그 생성됨.
    // -> 리스트에서 삭제됨
    // -> 필터에 맞는 레시피만 디스플레이됨
    // 태그는 닫기 버튼 있어야함. 
    

}